# SmartP 三步部署

> 部署目标：`39.107.61.117`（公网阿里云，root）
> 内网 IP：`172.26.55.198`
> 整理日期：2026-09-18

**这一步到位的原则**：不搞一键部署。底下三层分开起、分开验收，出问题一眼能看出是哪一层。

```
第1步-基础设施   →  第2步-前端  →  第3步-后端
（数据库/向量库/监控）  （nginx 静态页）  （FastAPI + MCP）
```

---

## 一分钟总览

| 步骤 | 放哪里 | 干什么 | 依赖 |
|---|---|---|---|
| **第 1 步** | `/root/` | `docker compose up -d` 起 7 个基础设施容器 | 先加 swap、建 `smartp-net` 网络 |
| **第 2 步** | `/opt/smartp/frontend/` | 起 nginx 容器，占 80 端口 | 网络要在 |
| **第 3 步** | `/opt/smartp/backend/` | 构建镜像 + 起后端容器 | 第 1 步的 Milvus 必须已经 ready |

**三步之间只共享一样东西**：`smartp-net` 这个 docker 网络（第 1 步里建）。

---

## 端口表（记不住就看这张）

| 服务 | 宿主机端口 | 说明 |
|---|---|---|
| nginx（前端） | **80** | 对外入口，`http://39.107.61.117` |
| 后端 FastAPI | （不对外） | 只在 `smartp-net` 内，nginx 用 `smartp-backend:9900` 访问 |
| Milvus | **19530** / 9091 | 19530 数据口，9091 健康口 |
| Prometheus | **9190** | → 容器内 9090（宿主机的 9090 被别的服务占了）|
| Alertmanager | 9093 | |
| Node Exporter | 9100 | |
| PostgreSQL | 5432 | postgres / 123321 / appdb |
| Redis | 6379 | 密码 123321 |
| Neo4j | 7474 / 7687 | neo4j / 12344321 |

---

## 上服务器的完整顺序

```bash
# ── 前提 1：加 swap（必做，1.7G 内存扛不住）────────────────
free -m                                    # 确认 Swap 是 0
fallocate -l 4G /swapfile && chmod 600 /swapfile && mkswap /swapfile && swapon /swapfile
echo '/swapfile none swap sw 0 0' >> /etc/fstab
sysctl -w vm.swappiness=10

# ── 前提 2：建共享网络（只建一次）──────────────────────────
docker network create smartp-net


# ══ 第 1 步：基础设施 ══════════════════════════════════════
#   把「第1步-基础设施」里的 4 个文件传到 /root/
#   ⚠️ 先处理 alert_rules.yml：服务器上那个路径现在是【目录】，要删掉换成文件
ls -la /root/alert_rules.yml               # 确认是空目录
rm -rf /root/alert_rules.yml               # 确认后删掉
#   然后把本地的 alert_rules.yml 传过去

chown -R 65534:65534 /root/prometheus_data # Prometheus 以 nobody 身份跑
cd /root && docker compose up -d

# 验收：7 个容器 Up，Milvus 健康
docker ps --format '{{.Names}}|{{.Status}}'
curl -s http://172.26.55.198:9091/healthz && echo " <- Milvus OK"
#   Milvus 要等 1~2 分钟才 ready，别急


# ══ 第 2 步：前端 ══════════════════════════════════════════
#   把「第2步-前端」传到 /opt/smartp/frontend/
cd /opt/smartp/frontend && docker compose up -d

curl -s -o /dev/null -w 'page=%{http_code}\n' http://172.26.55.198/   # 200 就行
#   （这时候调 /api 会 502，正常 —— 后端还没起）


# ══ 第 3 步：后端 ══════════════════════════════════════════
#   把「第3步-后端」传到 /opt/smartp/backend/
cd /opt/smartp/backend
docker compose build                        # 要几分钟
docker compose up -d

# 验收
docker exec smartp-backend pip list | grep -E '^(mcp|fastmcp|langchain-mcp-adapters) '
docker logs --tail 40 smartp-backend        # 要看到 Milvus 连接成功 + Uvicorn running

# 🔴 最后重启一次前端，让 nginx 重新解析后端地址
docker restart smartp-frontend

curl -s -o /dev/null -w 'health=%{http_code}\n' http://172.26.55.198/health   # 200 才算成
```

浏览器打开 `http://39.107.61.117/` 验收：问一句看流式输出、点「AI Ops 诊断」、上传个文件看切片。

---

## 五个最容易踩的坑

| # | 坑 | 后果 |
|---|---|---|
| 1 | **容器里的 `localhost` 不是虚拟机** | 容器有自己的网络命名空间。Milvus / Prometheus 在别的容器里，写 `localhost` 必连不上（实测容器内 127.0.0.1 上一个服务都没有，宿主机上却有 7 个）。所以 `.env` 里用 `172.26.55.198` |
| 2 | **不能用公网 IP 连自己** | 实测 `39.107.61.117:9190` 从容器里连是超时（errno=11），需要 NAT 回流，阿里云默认不支持。用内网 IP |
| 3 | **改了 `.env` 必须 `--force-recreate`** | compose 的 `env_file` 是建容器时注入的环境变量，而 pydantic-settings 里环境变量优先级**高于** `.env` 文件。只 `restart` = 白改 |
| 4 | **nginx 只在启动时解析 upstream** | 后端起来之后要 `docker restart smartp-frontend`，否则一直是 502 |
| 5 | **`/root/alert_rules.yml` 是个目录** | 挂进容器后 Prometheus 读不到规则。要删掉目录换成文件 |

---

## 和上一轮比，改了什么

| 项 | 上一轮 | 这一轮 |
|---|---|---|
| 部署方式 | 一个 compose 管前后端 | 拆成 3 步，各自独立 |
| 网络 | 引用了一个**不存在的** `milvus` 外接网络，compose 直接报错；容器最后掉在默认 bridge 上 | 统一个 `smartp-net`，第 1 步建好 |
| Milvus 连接 | `localhost`（容器自己） | `172.26.55.198`（虚拟机内网 IP） |
| Prometheus 连接 | `localhost:9190` | `172.26.55.198:9190` + 修了数据目录属主 |
| Alertmanager webhook | 指向 `192.168.150.1`（你本机，公网够不着） | 指向 `smartp-backend:9900`（同网络容器名）|
| 依赖 | 没锁上限，装到不兼容的 mcp 2.x | `pyproject.toml` 锁死 `mcp<2.0.0` |
| 容器重启策略 | `restart: no`（死了不自愈） | 全部 `unless-stopped` |
| swap | 无 | 4G |

---

## 目录对照

```
桌面/SmartP-deploy/
├── 总说明.md              ← 你正在看的这份
├── 第1步-基础设施/
│   ├── 说明.md
│   ├── docker-compose.yml     → /root/docker-compose.yml
│   ├── prometheus.yml         → /root/prometheus.yml
│   ├── alertmanager.yml       → /root/alertmanager.yml
│   └── alert_rules.yml        → /root/alert_rules.yml
├── 第2步-前端/
│   ├── 说明.md
│   ├── docker-compose.yml     → /opt/smartp/frontend/
│   ├── nginx.conf             → /opt/smartp/frontend/
│   └── static/                → /opt/smartp/frontend/static/
└── 第3步-后端/
    ├── 说明.md
    └── （整份）                → /opt/smartp/backend/
```

> 桌面上的旧目录 `SmartP/` **我没有删**，等你验收通过后自己清理。
> 注意别把两边的文件混着用——旧的 `SmartP/docker-compose.yml` 里还有那个不存在的 `milvus` 网络。

---

## 内存这笔账

1.7GB 内存 + 无 swap，要扛 9 个容器，**仍然很紧**。加 swap 是保底不是根治。

实测占用：`neo4j 348MB`、`milvus_minio 96MB`、`postgres 19MB`、`milvus_etcd 16MB`、`alertmanager 13MB`、`redis 4MB`、`node_exporter 3MB`（Milvus 本体和 Prometheus 起来后还要各加几百 MB）。

**如果 Milvus 还是反复死**，第一个该砍的是 **Neo4j**——SmartP 的依赖里根本没有 neo4j，是别的用途。把它在 compose 里整段注释掉，能省 ~350MB：

```bash
# 注释掉 neo4j 那段后
cd /root && docker compose up -d --remove-orphans
```

再不行就得考虑升级内存，或者把 Milvus 单独挪到别的机器。